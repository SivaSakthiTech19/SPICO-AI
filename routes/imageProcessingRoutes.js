const express = require("express");
const multer = require("multer");

const router = express.Router();
const upload = multer();

// Removed removeBackground controller and route as per request

module.exports = router;
